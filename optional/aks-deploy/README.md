# optional/aks-deploy — superseded by optional/eks-deploy

The Kubernetes + WordPress + EDL add-on was ported to AWS as
**`optional/eks-deploy/`** (AKS→EKS, Front Door→CloudFront, Azure Service Tags→
AWS `ip-ranges.json`). See `optional/eks-deploy/README.md`. This directory is
kept only as a pointer.
